typedef enum {
    CONTROL_MODE_JOYSTICK,
    CONTROL_MODE_GRAVITY,
    CONTROL_MODE_OBSTACLE_AVOIDANCE,
} control_mode_t;

typedef enum {
    RGB_MODE_BREATHING,
    RGB_MODE_RUNNING,
    RGB_MODE_FLASHING,
} rgb_mode_t;